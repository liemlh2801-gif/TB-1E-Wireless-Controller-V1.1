# Legacy Arduino Nano + HC-05

This project now uses **ESP32** built-in Bluetooth instead of Arduino Nano + HC-05.

Use the firmware in [`../esp32/bt_controller/`](../esp32/bt_controller/).

The old `bt_controller.ino` for Nano/HC-05 has been removed.
